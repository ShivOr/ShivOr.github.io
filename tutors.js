const grid=document.getElementById("tutor-grid"),filters=document.getElementById("subject-filters");let activeSubject="All";
function tutorCard(t){return `<article class="tutor-card"><img class="tutor-photo" src="${t.photo}" alt="Illustrated portrait of ${t.name}"><div class="tutor-body"><div class="tutor-top"><div><h3>${t.name}</h3><p>${t.grades} grades</p></div><div class="rate">$${t.rate}/hr</div></div><div class="tags">${t.subjects.map(s=>`<span class="tag">${s}</span>`).join("")}</div><p class="availability"><strong>Next openings:</strong> ${t.availability.join(" · ")}</p><a class="btn btn-primary btn-wide tutor-view-link" href="book.html?tutor=${t.id}" data-tutor-id="${t.id}" data-tutor-name="${t.name}">View & book ${t.name.split(" ")[0]}</a></div></article>`}
function render(){const shown=activeSubject==="All"?ABC_TUTORS:ABC_TUTORS.filter(t=>t.subjects.includes(activeSubject));grid.innerHTML=shown.map(tutorCard).join("");grid.querySelectorAll(".tutor-view-link").forEach(link=>link.addEventListener("click",()=>analytics.capture("tutor_profile_viewed",{tutor_id:link.dataset.tutorId,tutor_name:link.dataset.tutorName,source_page:"tutors"})));}
["All",...ABC_SUBJECTS].forEach(subject=>{const btn=document.createElement("button");btn.className="filter-btn"+(subject==="All"?" active":"");btn.textContent=subject;btn.addEventListener("click",()=>{activeSubject=subject;[...filters.children].forEach(x=>x.classList.remove("active"));btn.classList.add("active");analytics.capture("subject_interest",{subject,source_page:"tutors_filter"});render();});filters.appendChild(btn);});render();
